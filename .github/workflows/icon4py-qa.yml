name: ICON4Py Quality Assurance

on:
    workflow_dispatch:
    push:
        branches:
            - main
        paths-ignore:
            - "tools/**"
    pull_request:
        branches:
            - main
        paths-ignore:
            - "tools/**"
        types: [opened, reopened, assigned, ready_for_review]
    pull_request_review:
        branches:
            - main
        paths-ignore:
            - "tools/**"
        types: [submitted]

jobs:
    pre-commit-icon4py-model-common:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            -   name: Install system dependencies
                run: |
                    sudo apt-get update
                    sudo apt-get install libboost-all-dev
            - name: Set up Python
              uses: actions/setup-python@v4
              with:
                  python-version: "3.10"
                  cache: 'pip'
                  cache-dependency-path: |
                    **/pyproject.toml
                    **/base-requirements.txt
                    **/base-requirements-dev.txt
                    **/requirements.txt
                    **/requirements-dev.txt
            - name: Install icon4py-common
              working-directory: model/common
              run: |
                  python -m pip install --upgrade pip setuptools wheel
                  python -m pip install -r ./requirements-dev.txt
                  python -m pip list
            - name: Run checks icon4py-model-common
              run: |
                  pre-commit run --config model/common/.pre-commit-config.yaml --all-files

    pre-commit-icon4py-model-driver:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            -   name: Install system dependencies
                run: |
                    sudo apt-get update
                    sudo apt-get install libboost-all-dev
            - name: Set up Python
              uses: actions/setup-python@v4
              with:
                  python-version: "3.10"
                  cache: 'pip'
                  cache-dependency-path: |
                    **/pyproject.toml
                    **/base-requirements.txt
                    **/base-requirements-dev.txt
                    **/requirements.txt
                    **/requirements-dev.txt
            - name: Install icon4py-model-driver
              working-directory: model/driver
              run: |
                  python -m pip install --upgrade pip setuptools wheel
                  python -m pip install -r ./requirements-dev.txt
            - name: Run checks icon4py-model-driver
              run: |
                  pre-commit run --config model/driver/.pre-commit-config.yaml --all-files

    pre-commit-icon4py-model-atmosphere-dycore:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Install system dependencies
              run: |
                  sudo apt-get update
                  sudo apt-get install libboost-all-dev
            - name: Set up Python
              uses: actions/setup-python@v4
              with:
                  python-version: "3.10"
                  cache: 'pip'
                  cache-dependency-path: |
                    **/pyproject.toml
                    **/base-requirements.txt
                    **/base-requirements-dev.txt
                    **/requirements.txt
                    **/requirements-dev.txt
            - name: Install icon4py-model-atmosphere-dycore
              working-directory: model/atmosphere/dycore
              run: |
                  python -m pip install --upgrade pip setuptools wheel
                  python -m pip install -r ./requirements-dev.txt
            - name: Run checks icon4py-model-atmosphere-dycore
              run: |
                  pre-commit run --config model/atmosphere/dycore/.pre-commit-config.yaml --all-files

    pre-commit-icon4py-model-atmosphere-diffusion:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            -   name: Install system dependencies
                run: |
                    sudo apt-get update
                    sudo apt-get install libboost-all-dev
            - name: Set up Python
              uses: actions/setup-python@v4
              with:
                  python-version: "3.10"
                  cache: 'pip'
                  cache-dependency-path: |
                    **/pyproject.toml
                    **/base-requirements.txt
                    **/base-requirements-dev.txt
                    **/requirements.txt
                    **/requirements-dev.txt
            - name: Install icon4py-model-atmosphere-diffusion
              working-directory: model/atmosphere/diffusion
              run: |
                  python -m pip install --upgrade pip setuptools wheel
                  python -m pip install -r ./requirements-dev.txt
            - name: Run checks icon4py-model-atmosphere-diffusion
              run: |
                  pre-commit run --config model/atmosphere/diffusion/.pre-commit-config.yaml --all-files
