# icon4py-atmosphere-physics-driver

The physics driver (`PhysicsDriver`) and its process registry (`PhysicsProcess`, `ProcessTimeControl`, `ForcingMode`) for ICON4Py, modelled on ICON-MPIM's AES physics interface (`aes_phy_main`)
